---
aliases: [{service-name}, {service-name-spanish}, Service Overview]
type: microservice
layer: Backend
status: active
owner: "[[TBD]]"
tech_stack: "[[Technology]]"
audience: [microservices]
last_updated: "{YYYY-MM-DD}"
source_branch: "{branch}"
commit_range: "{from-sha}..{to-sha}"
last_documented_sha: "{full-sha}"
doc_version: "1.0.0"
---

# {service-name} — Service Overview

> One-paragraph summary: what this service does, why it exists, and what systems it connects.

## Overview

### Purpose
What business problem does this service solve? Why does it exist as a separate service?

### Position in Architecture
Where does this service sit in the overall system? What calls it? What does it call?

```mermaid
graph LR
    A[Caller Service / External] -->|HTTP/Queue| B[{service-name}]
    B -->|HTTP/Queue| C[Downstream Service / External]
```

## API / Endpoints

> Document every exposed endpoint. If this service doesn't expose HTTP endpoints (e.g., it's a queue consumer only), note that and skip to the relevant section.

### REST Endpoints

| Method | Path | Description | Auth | Request Body | Response |
|--------|------|-------------|------|-------------|----------|
| GET | /health | Health check | None | — | `{ status: "ok" }` |

### Queue Consumers

| Queue/Topic | Message Type | Description | Retry Policy |
|-------------|-------------|-------------|--------------|

### Queue Producers

| Queue/Topic | Message Type | When Published | Payload Shape |
|-------------|-------------|----------------|---------------|

### Cron Jobs / Scheduled Tasks

| Schedule | Task | Description |
|----------|------|-------------|
| — | — | N/A — no cron jobs in this service |

## Modules / Internal Architecture

> Describe the internal organization of the service. One subsection per major module or domain area.

### {module-name}
- **Responsibility**: What this module does
- **Key files**: `src/modules/{module}/`
- **Dependencies**: What other modules or external services it uses
- **Notable patterns**: Any non-obvious design choices

## External Integrations

> Detail every external system this service communicates with.

### {integration-name}
- **Type**: REST API / SOAP / SDK / Database / Queue
- **Direction**: Inbound / Outbound / Both
- **Authentication**: How auth is handled
- **Key operations**: What specific operations are used
- **Error handling**: How failures are managed
- **Rate limits / Throttling**: Known limits
- **Documentation link**: URL to external API docs if available

## Configuration

### Environment Variables

| Variable | Required | Default | Description |
|----------|----------|---------|-------------|
| `NODE_ENV` | Yes | — | Runtime environment |

### Feature Flags

| Flag | Default | Description |
|------|---------|-------------|

## Data Models

> Document the key data structures, database schemas, or DTOs that define this service's domain.

### {model-name}
```typescript
// Source: src/models/{model}.ts
interface Example {
  id: string;
  // ...
}
```

## Error Handling

### Error Categories
- **Retryable**: Which errors trigger automatic retries?
- **Fatal**: Which errors require manual intervention?
- **Notification**: Which errors trigger alerts?

### Common Failure Modes
| Failure | Symptom | Resolution |
|---------|---------|------------|

## Infrastructure

### Deployment
- **Platform**: K8s / Cloud Run / etc.
- **Replicas**: Number of instances
- **Resources**: CPU/memory limits if known

### Dependencies
- **Runtime**: Node.js version, etc.
- **Databases**: Redis, Postgres, etc.
- **Queues**: Redis, Pub/Sub, etc.

### Monitoring
- Health check endpoint: `GET /health`
- Key metrics to watch:
- Logs: where to find them

## Known Issues & Tech Debt

> Document known problems, workarounds, and planned improvements. This section is critically valuable for onboarding.

| Issue | Impact | Workaround | Planned Fix |
|-------|--------|------------|-------------|

## Changelog

> Reverse chronological. Each entry references the commits it covers.

### [{YYYY-MM-DD}] — Initial documentation
- Generated by living-docs from commits `{from-sha}` to `{to-sha}`
- Covers: {brief description of scope}
